# Example variable values for LOCAL testing only.
# Copy to a private terraform.tfvars (which is gitignored) and fill in,
# OR pass values via TF_VAR_* environment variables in CI. Never commit secrets.

# vercel_api_token = "xxxxxxxxxxxxxxxxxxxx"   # from https://vercel.com/account/tokens
# vercel_team_id   = ""                        # "" for personal account, else "team_xxx"
# project_name     = "earthquake-monitoring-dashboard"
# vite_api_url     = "https://your-backend.example.com/api"
